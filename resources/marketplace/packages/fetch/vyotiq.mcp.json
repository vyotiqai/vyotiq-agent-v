{
  "schemaVersion": 1,
  "kind": "mcp",
  "id": "fetch",
  "name": "Fetch",
  "version": "1.0.0",
  "description": "Official MCP fetch server (stdio via uvx). Requires uv on PATH. Pins mcp<2 for SDK compatibility.",
  "transport": "stdio",
  "command": "uvx",
  "args": ["--with", "mcp<2", "mcp-server-fetch"],
  "env": {
    "PYTHONIOENCODING": "utf-8"
  }
}
